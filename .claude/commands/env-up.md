Start the Next.js dev server with Turbopack in the background:

```bash
cd /Users/fernando/Development/pomodoro-demo-ccode && pnpm dev
```

Run this in the background so the user can continue working. Confirm the server is running on http://localhost:3000.
