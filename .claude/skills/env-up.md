---
name: env-up
description: Use when the user wants to start the development environment, run the dev server, or begin working on the project
---

# Start Development Environment

Start the Next.js dev server with Turbopack.

```bash
cd /Users/fernando/Development/pomodoro-demo-ccode
pnpm dev
```

Run this in the background so the user can continue working. Confirm the server is running on http://localhost:3000.
